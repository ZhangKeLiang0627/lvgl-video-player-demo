# Build artifacts (device Makefile)
*.o
demo

# Build artifacts (CMake)
build/
cmake-build-*/

# Editor / OS noise
*.swp
.DS_Store
.vscode/
.idea/

# LVGL is tracked as a git submodule (only its gitlink/pointer is committed).

# Debug backup binaries
*.bak.*
demo.bak
